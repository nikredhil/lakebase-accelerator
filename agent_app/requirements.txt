fastapi>=0.110
uvicorn>=0.29
databricks-sdk>=0.38
# Live agent: Lakebase Postgres memory + LangGraph checkpointer. Imported
# lazily so the app still boots if a wheel is unavailable.
psycopg[binary]>=3.1
langgraph>=0.2
langgraph-checkpoint-postgres>=2.0
# Trace spans (agent_chat > embed/recall/llm_call) to the workspace MLflow
# experiment named by MLFLOW_EXPERIMENT. Best-effort at runtime — see agent.py.
mlflow-skinny>=3.1
